services:
  beszel:
    image: henrygd/beszel:latest
    container_name: beszel
    restart: unless-stopped
    ports:
      - 5001:8090
    volumes:
      - ./data:/beszel_data
      - ./socket:/beszel_socket
networks: {}
