services:
  searxng:
    image: searxng/searxng:latest
    restart: unless-stopped
    volumes:
      - ./config/:/etc/searxng/
    ports:
      - 5104:8080
    env_file:
      - ./.env
    container_name: searxng
networks: {}
